{
  "name": "chemintel",
  "version": "1.0.0",
  "private": true,
  "description": "CHEMINTEL - AI Powered Computational Drug Discovery Platform",
  "scripts": {
    "dev": "concurrently \"pnpm run server\" \"pnpm run client\"",
    "client": "pnpm --dir frontend run dev",
    "server": "pnpm --dir backend run dev",
    "install:all": "pnpm install && pnpm --dir frontend install && pnpm --dir backend install",
    "build": "pnpm --dir frontend run build",
    "start": "pnpm --dir backend start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
